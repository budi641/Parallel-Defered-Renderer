# Parallel-Deferred-Renderer Build Configuration
# Edit these values to customize your build

# Project Configuration
PROJECT_NAME=Parallel-Deferred-Renderer
DEFAULT_CONFIG=Debug

# Build Options
CLEAN_BUILD_ON_START=false
RUN_AFTER_BUILD=false
VERBOSE_BUILD=false

# Compiler Options
CMAKE_GENERATOR=Visual Studio 16 2019
CMAKE_ARCHITECTURE=x64

# Paths
BUILD_DIR_PREFIX=build_
RESOURCES_DIR=resources

# Debug Options
DEBUG_SYMBOLS=true
DEBUG_INFO=ON

# Release Options
RELEASE_OPTIMIZATION=ON
RELEASE_STRIP_SYMBOLS=false

# Dependencies
REQUIRED_PACKAGES=OpenGL,GLFW,GLM,Assimp,STB,ImGui

# Build Flags
CMAKE_CXX_FLAGS_DEBUG=-g -O0 -Wall -Wextra
CMAKE_CXX_FLAGS_RELEASE=-O3 -DNDEBUG -Wall -Wextra
